import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'RGES India - Modern Business Solutions',
  description: 'Leading business consulting and solutions provider with innovative approaches to drive your success.',
  keywords: 'business consulting, solutions, innovation, technology, RGES India',
  authors: [{ name: 'RGES India' }],
  viewport: 'width=device-width, initial-scale=1',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.className} antialiased`}>
        {children}
      </body>
    </html>
  )
} 